<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.jarvis.mapper.UserMapper">

	<resultMap id="UserResultMap" type="com.example.jarvis.model.SysUser">
		<id     property="id"            column="id"/>
		<result property="username"      column="username"/>
		<result property="passwordHash"  column="password_hash"/>
		<result property="nickname"      column="nickname"/>
		<result property="email"         column="email"/>
		<result property="role"          column="role"/>
		<result property="status"        column="status"/>
		<result property="avatarUrl"     column="avatar_url"/>
		<result property="lastLoginAt"   column="last_login_at"/>
		<result property="createdAt"     column="created_at"/>
		<result property="updatedAt"     column="updated_at"/>
	</resultMap>

	<sql id="BaseColumns">
		id, username, password_hash, nickname, email, role, status,
		avatar_url, last_login_at, created_at, updated_at
	</sql>

	<!-- 根据用户名查询 -->
	<select id="findByUsername" parameterType="string" resultMap="UserResultMap">
		SELECT
		<include refid="BaseColumns"/>
		FROM sys_user
		WHERE username = #{username}
		LIMIT 1
	</select>

	<!-- 根据ID查询 -->
	<select id="findById" parameterType="long" resultMap="UserResultMap">
		SELECT
		<include refid="BaseColumns"/>
		FROM sys_user
		WHERE id = #{id}
		LIMIT 1
	</select>

	<!-- 插入新用户 -->
	<insert id="insert" parameterType="com.example.jarvis.model.SysUser" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO sys_user (
			username, password_hash, nickname, email,
			role, status, avatar_url
		)
		VALUES (
			#{username}, #{passwordHash}, #{nickname}, #{email},
			#{role}, #{status}, #{avatarUrl}
		)
	</insert>

	<!-- 更新用户基本信息 -->
	<update id="update" parameterType="com.example.jarvis.model.SysUser">
		UPDATE sys_user
		SET
			nickname   = #{nickname},
			email      = #{email},
			role       = #{role},
			status     = #{status},
			avatar_url = #{avatarUrl}
		WHERE id = #{id}
	</update>

	<!-- 更新最近登录时间 -->
	<update id="updateLastLoginAt" parameterType="long">
		UPDATE sys_user
		SET last_login_at = CURRENT_TIMESTAMP
		WHERE id = #{id}
	</update>

	<!-- 查询所有用户 -->
	<select id="findAll" resultMap="UserResultMap">
		SELECT
		<include refid="BaseColumns"/>
		FROM sys_user
		ORDER BY id
	</select>

	<!-- 检查用户名是否存在 -->
	<select id="countByUsername" parameterType="string" resultType="int">
		SELECT COUNT(1)
		FROM sys_user
		WHERE username = #{username}
	</select>

</mapper>
